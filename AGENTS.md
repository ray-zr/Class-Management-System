# 智慧班级综合管理系统 Agent 指南

本文件是 AI Agent 在本仓库工作的入口。先按任务范围读取相关源文件，保持改动最小，并以仓库内代码和文档为事实来源。

## 1. 项目概览

- 技术栈：Go 1.24.2、go-zero REST、GORM、MySQL 8；前端为原生 HTML/CSS/JavaScript。
- 运行形态：Nginx 提供静态前端并反向代理 `/api/`，Go 服务监听 8888，MySQL 持久化业务数据。
- 主要能力：登录鉴权、学生与小组管理、积分维度与积分项、积分记录、排行榜、随机点名、Excel 导入导出。
- Go module 为 `class-management-system`，后端入口是 `backend/cms.go`。

> 依据：`go.mod:1-11`、`backend/cms.go:31-87`、`deploy/nginx/default.conf:23-35`、`docs/cms.api:194-260`。

## 2. 事实来源与阅读顺序

1. 接口字段和端点从 `docs/cms.api` 开始，实际路由核对 `backend/internal/handler/routes.go`。
2. 请求链路按 `handler -> logic -> repository -> model/MySQL` 阅读；共享依赖由 `svc.ServiceContext` 组装。
3. 配置结构以 `backend/internal/config/config.go` 为准，部署值核对 `backend/etc/*.yaml` 与 `docker-compose.yml`。
4. 数据表范围核对 `backend/internal/model/` 和 `backend/internal/db/migrate.go`。
5. 部署和故障处理先读 `docs/ops.md`，涉及恢复、备份或账号变更时读 `docs/ops_runbook.md`。
6. 用户操作说明读 `docs/teacher_guide.md`，总体实现背景读 `docs/design_impl.md`。

> 依据：`backend/internal/handler/routes.go:14-174`、`backend/internal/svc/servicecontext.go:16-51`、`backend/internal/db/migrate.go:11-21`。

## 3. 目录职责

| 路径 | 职责 |
| --- | --- |
| `backend/cms.go` | 加载配置、覆盖认证环境变量、迁移数据库、挂中间件、启动清理任务和 HTTP 服务 |
| `backend/internal/handler/` | HTTP 参数解析、调用 logic、写响应；不承载业务规则 |
| `backend/internal/logic/` | 输入校验、业务编排、事务边界和响应转换 |
| `backend/internal/repository/` | GORM 查询、持久化、锁和聚合查询 |
| `backend/internal/model/` | 数据库模型 |
| `backend/internal/types/` | API 请求/响应类型，由 goctl 生成 |
| `backend/internal/auth/`、`middleware/` | JWT 创建解析、身份注入和路由鉴权 |
| `backend/internal/retention/`、`rollcall/` | 定时清理与点名状态 |
| `docs/frontend/` | 无构建步骤的静态前端与导入模板 |
| `deploy/nginx/` | 静态资源服务和 `/api/` 反向代理配置 |
| `tools/` | 开发辅助程序，不属于在线服务入口 |

> 依据：`backend/internal/handler/studentcreatehandler.go:15-30`、`backend/internal/logic/studentcreatelogic.go:32-59`、`backend/internal/repository/student_repo.go:14-110`。

## 4. 修改规则

- 尊重现有分层：handler 只处理 HTTP，业务判断放 logic，数据库访问放 repository。
- 新增或修改 API 时先改 `docs/cms.api`，同步核对生成的路由、类型、handler、logic 及前端调用。
- 不手工编辑带 `Code generated by goctl. DO NOT EDIT.` 的文件，当前包括 `backend/internal/handler/routes.go` 和 `backend/internal/types/types.go`。
- 带 `Code scaffolded by goctl. Safe to edit.` 的 handler、logic、config、service context 可维护，但重新生成前必须检查未提交差异。
- 模型变化必须同步检查 `db.AutoMigrate`、相关 repository 查询、导入导出和既有数据兼容性；禁止假定破坏性迁移安全。
- 错误使用 `backend/internal/httperr` 表达预期 HTTP 错误；不要把密钥、密码、完整 DSN 或 token 写入日志和响应。
- 保持现有 JSON 字段、状态码和时间/分页语义兼容；确需破坏性变更时必须明确说明影响。
- 不顺手重构无关文件，不覆盖用户已有改动，不提交生成物或依赖缓存，除非任务明确要求。
- 新增行为应配套最小、针对性的 `*_test.go`；当前仓库没有自动化测试文件，不能把“可编译”等同于行为已验证。

> 依据：`backend/internal/types/types.go:1-2`、`backend/internal/handler/routes.go:1-2`、`backend/cms.go:50-63`。

## 5. 本地检查

在仓库根目录执行：

```bash
gofmt -w <本次修改的.go文件>
go test ./...
go vet ./...
go build ./...
```

- 只对本次修改的 Go 文件运行 `gofmt`，避免无关格式化噪音。
- 涉及 API 时至少核对 `docs/cms.api` 与 `routes.go`、`types.go` 一致。
- 涉及前端时检查窄屏和桌面布局、登录过期、空数据、请求失败及下载/导入流程。
- 涉及数据库时在隔离数据上验证迁移与查询；不得用生产数据做试验。
- 完成时说明运行过的命令、结果，以及未能执行的验证和剩余风险。

> 构建依据：`backend/Dockerfile:1-16`。目前没有 Makefile、CI 工作流或项目专用 lint 脚本，不要声称运行过它们。

## 6. 部署方式

标准部署使用根目录 `docker-compose.yml`，包含 `mysql`、`cms-api`、`web` 三个服务；数据库使用具名卷 `mysql_data`。

```bash
docker compose up -d --build
docker compose ps
curl -i http://127.0.0.1/api/health
docker compose logs --tail=200 cms-api
```

- Web 默认暴露宿主机 80 端口；MySQL 仅绑定 `127.0.0.1:13306`。
- `docker compose down` 停止服务；除非用户明确要求永久清空数据，不得执行 `docker compose down -v`。
- 认证可由 `CMS_AUTH_USERNAME`、`CMS_AUTH_PASSWORD_HASH` 或 `CMS_AUTH_PASSWORD` 覆盖；不要在输出中复述真实值。
- 不复制示例密钥到生产环境。修改 JWT 密钥会使现有 token 失效，部署说明中必须提示这一点。

> 依据：`docker-compose.yml:1-44`、`backend/cms.go:38-49`、`docs/ops.md:18-31`、`docs/ops_runbook.md:16-24`。

## 7. 绘图要求

- 有绘图需求时优先 draw.io，其次 Mermaid，以用户指定格式为准。
- draw.io 必须生成可由 diagrams.net 打开编辑的 `*.drawio.svg`，不能把普通 SVG 改名冒充。
- 文件按图形内容使用功能性中文名称；每张图一个文件，不使用多页面。
- 图片保存到引用它的 Markdown 同名 `.assets/` 目录。
- Markdown 使用 `![图形说明](<./文档名.assets/功能性文件名.drawio.svg>)` 引用。
- 完成后校验 SVG XML 合法、内嵌 `mxfile` 可解析、Markdown 链接路径正确。

## 8. 完成检查

1. `git diff --check` 无空白错误，且差异只覆盖任务范围。
2. Go 改动已格式化，并运行 `go test ./...`、`go vet ./...`、`go build ./...`。
3. API、配置、数据库或部署变化已同步更新对应事实来源和用户文档。
4. 未泄露凭据，未执行破坏性数据操作，未修改无关文件。
5. 最终回复简述变更、验证结果和任何未覆盖风险。
