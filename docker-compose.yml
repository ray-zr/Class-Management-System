services:
  mysql:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: cms
      MYSQL_USER: cms
      MYSQL_PASSWORD: cms
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "127.0.0.1:13306:3306"
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_0900_ai_ci
    volumes:
      - mysql_data:/var/lib/mysql

  cms-api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    depends_on:
      - mysql
    restart: unless-stopped
    volumes:
      - ./backend/etc/cms-api.docker.yaml:/app/etc/cms-api.yaml:ro
    environment:
      TZ: Asia/Shanghai
      CMS_AUTH_USERNAME: "${CMS_AUTH_USERNAME:?CMS_AUTH_USERNAME is required}"
      CMS_AUTH_PASSWORD_HASH: "${CMS_AUTH_PASSWORD_HASH:?CMS_AUTH_PASSWORD_HASH is required}"
      CMS_AUTH_JWT_SECRET: "${CMS_AUTH_JWT_SECRET:?CMS_AUTH_JWT_SECRET is required}"

  web:
    image: nginx:1.27-alpine
    depends_on:
      - cms-api
    restart: unless-stopped
    ports:
      - "0.0.0.0:80:80"
    volumes:
      - ./docs/frontend:/usr/share/nginx/html:ro
      - ./deploy/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro

volumes:
  mysql_data:
